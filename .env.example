# Insight API benchmarking
# Copy this file to .env and fill in your values

# Required: thirdweb client id for x-client-id header
X_CLIENT_ID=

# Optional: override base API URL (defaults to https://insight.thirdweb.com/v1)
BASE_URL=https://insight.thirdweb.com/v1

# Optional: sleep between pages (ms) to avoid rate limits
SLEEP_MS=0
